# Public site origin for server-side ofetch when resolving `/api/load` and `/api/save` (optional).
# Defaults: development → http://localhost:3000, production build → https://shaqirivarlind.github.io
# NUXT_PUBLIC_SITE_URL=http://localhost:3000
